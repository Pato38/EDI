viajes=dict
viajes={'dia':int(input()),'importe':int(input()),'distancia':int(input())}

i=0
def vector_v(importe,distancia,dia):
	if(viajes['distancia'>0] or (dimf==200)):
		for i in range(0,31):
			total_viaje['importe']=viaje['importe']+i
			prom=total_viaje*0,31
			return(prom)
		dis_rec=1
		dmax=1
		for viajes['distancia'] in range(viajes):
			dis_rec=viajes['distancia']+1
			return(dis_rec)

		importe_max=1
		for viajes['importe'] in range(1,31):
			if(viajes['importe']>importe_max):
				importe_max=viajes['importe']
				
				viajes['importe']=viajes['dia']
				return(viajes['dia'])

def v_menores(distancia):
	if(viajes['distancia']==100):
		viajes['distancia']=0
		return()


		
