ubuntu=dict
ubuntu={'version':int(input()),'paq':int(input()),'cta':int(input())}
while(ubuntu['version']<4.10):
	if(ubuntu['version']==18.04) or (ubuntu['version'==16.04]):
		cant_max=1
		cant_max=cant_max+1
		print("la cantidad de equipos que usan ubuntu, version 18.04 y 16.04 es:  ", cant_max)
	max_paq=1
	paq=1
	if(ubuntu['paq']>max_paq):
		max_paq=ubuntu['paq']
		if(max_paq==ubuntu['version']):
			print(ubuntu['version'])